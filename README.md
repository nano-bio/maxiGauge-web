# MaxiGauge Logger and Viewer for Labbooks AG Scheier

### Installation
pip -r requirements.txt

### Logger Path
Change the path in .env file (see .env.example)

### Troubles
If it does not work, provide a "COM_PORT" in .env